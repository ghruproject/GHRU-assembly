profiles {
    standard {
        docker.runOptions='--platform=linux/x86_64'
        docker.enabled = true
        //rework base
        //container = 'juliofdiaz/python-base:latest'
        //to be changed
        process {
            executor = 'local'
            withName: HYBRID_ASSEMBLY {
                container = 'biocontainer/relevant:container'
            }            

        }  
    }
}